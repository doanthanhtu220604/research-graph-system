# 🎓 Kịch bản Thuyết trình Bảo vệ Đồ án (Đọc chi tiết)
## Đề tài: XÂY DỰNG KNOWLEDGE MAP PHỤC VỤ NGHIÊN CỨU KHOA HỌC TẠI KHOA CNTT
**Thời lượng thiết kế: 12 – 15 phút** (Chỉ thuyết trình lý thuyết và giải pháp công nghệ, không bao gồm demo thực tế)

---

## ⏱️ Hướng dẫn phân bổ thời gian & tốc độ nói
* **Tốc độ đọc trung bình:** Khoảng 140 - 150 từ/phút. Bạn hãy đọc rõ ràng, nhấn nhá ở các từ khóa quan trọng và không đọc quá nhanh.
* **Thời lượng ước tính:** Khoảng 12 - 14 phút nếu đọc trôi chảy có dừng nghỉ tự nhiên.
* Các phần trong ngoặc vuông **[ ]** là hướng dẫn cử chỉ hoặc chuyển slide, bạn không đọc phần đó ra thành lời.

---

## 📌 PHẦN 1: Mở đầu & Đặt vấn đề (≈ 2.5 phút)
*(Số từ: ~350 từ)*

**[Slide 1: Slide Tiêu đề đồ án - Tên sinh viên, GVHD]**

**Lời thoại:**
> *"Kính thưa quý thầy cô trong Hội đồng bảo vệ và toàn thể các bạn sinh viên có mặt trong buổi ngày hôm nay.*
>
> *Em tên là [Họ và tên của bạn], hôm nay em xin phép được trình bày báo cáo đồ án tốt nghiệp của mình với đề tài: **XÂY DỰNG KNOWLEDGE MAP PHỤC VỤ NGHIÊN CỨU KHOA HỌC TẠI KHOA CNTT**.*
>
> *Đầu tiên, để bắt đầu bài thuyết trình, em xin phép đặt ra một tình huống thực tế thường gặp trong môi trường học thuật của chúng ta. Nếu một giảng viên, một sinh viên nghiên cứu, hoặc một đối tác bên ngoài muốn biết: 'Trong Khoa của chúng ta, những giảng viên nào đang nghiên cứu chuyên sâu về Học máy (Machine Learning)?', hay 'Ai đã từng cộng tác nghiên cứu với thầy Nguyễn Văn A trong 3 năm gần đây?' — Liệu chúng ta có thể có ngay câu trả lời trong vòng 5 giây không?*

**[Slide 2: Thực trạng & Tính cấp thiết]**

**Lời thoại:**
> *Thưa thầy cô, câu trả lời hiện tại là rất khó. Thực tế cho thấy, dữ liệu nghiên cứu khoa học của một đơn vị giáo dục hiện nay đang gặp phải ba vấn đề lớn:*
>
> * **Thứ nhất là sự phân tán dữ liệu:** Thông tin lý lịch giảng viên, bài báo quốc tế, đề tài nghiên cứu các cấp thường nằm rải rác. Có cái nằm trên website trường, có cái nằm trong các file Excel báo cáo của trợ lý khoa, có cái lại chỉ nằm trong hồ sơ cá nhân của từng giảng viên.*
> * **Thứ hai là thiếu tính kết nối trực quan:** Dữ liệu dạng bảng biểu truyền thống chỉ cho biết thông tin độc lập. Chúng ta hoàn toàn không thấy được bức tranh toàn cảnh về mạng lưới cộng tác giữa các giảng viên, cũng như sự giao thoa giữa các lĩnh vực nghiên cứu.*
> * **Thứ ba là phương thức tra cứu thủ công:** Việc thống kê hoặc tìm kiếm người có cùng hướng nghiên cứu để lập nhóm đề tài mới tốn rất nhiều thời gian và công sức.*
>
> *Chính vì những lý do cấp thiết đó, em đã nghiên cứu và phát triển hệ thống quản lý nhằm hiện thực hóa đề tài **Xây dựng Knowledge Map phục vụ nghiên cứu khoa học tại Khoa CNTT** nhằm chuyển đổi mô hình quản lý thông tin từ dạng bảng biểu truyền thống sang dạng **mạng lưới đồ thị tri thức**. Hệ thống không chỉ quản lý, hiển thị trực quan mà còn tích hợp Trí tuệ Nhân tạo hỗ trợ người dùng khai thác dữ liệu dễ dàng nhất."*

---

## 📌 PHẦN 2: Mục tiêu & Phạm vi đề tài (≈ 1.5 phút)
*(Số từ: ~220 từ)*

**[Slide 3: Mục tiêu & Phạm vi đề tài]**

**Lời thoại:**
> *Thưa thầy cô, từ việc xác định rõ các bài toán thực tế nêu trên, đồ án của em tập trung vào 4 mục tiêu cốt lõi sau:*
>
> * **Mục tiêu thứ nhất:** Thiết lập và trực quan hóa thành công một **Bản đồ Tri thức tương tác**. Bản đồ này sẽ biểu diễn giảng viên, công trình công bố, đề tài nghiên cứu khoa học và các bộ môn dưới dạng một mạng lưới kết nối trực quan, cho phép phóng to, thu nhỏ, lọc và tương tác thời gian thực.*
> * **Mục tiêu thứ hai:** Phát triển một **Chatbot AI thông minh** ứng dụng mô hình ngôn ngữ lớn để trả lời các câu hỏi về hoạt động nghiên cứu khoa học của Khoa bằng tiếng Việt tự nhiên.*
> * **Mục tiêu thứ ba:** Chuẩn hóa quy trình nghiệp vụ thông qua cơ chế phân quyền 3 tầng người dùng bao gồm Quản trị viên khoa, Giảng viên và Khách vãng lai.*
> * **Mục tiêu thứ tư:** Tự động hóa khâu xử lý dữ liệu đầu vào thông qua công cụ import hàng loạt từ Excel và quy trình kiểm duyệt thông tin 2 bước.*
>
> *Về mặt phạm vi, đề tài này giới hạn thử nghiệm dữ liệu thực tế tại Khoa Công nghệ Thông tin, Đại học Nha Trang, tập trung vào các thông tin khoa học cốt lõi như giảng viên cơ hữu, các công trình bài báo trong nước và quốc tế, đề tài NCKH các cấp và các mối quan hệ cộng tác thực tế.*

---

## 📌 PHẦN 3: Cơ sở lý thuyết: Tại sao lại chọn CSDL Đồ thị Neo4j? (≈ 3 phút)
*(Số từ: ~420 từ)*

**[Slide 4: So sánh CSDL Đồ thị (Neo4j) và CSDL Quan hệ (SQL)]**

**Lời thoại:**
> *Thưa thầy cô, một trong những điểm đặc biệt nhất của đồ án này nằm ở việc lựa chọn giải pháp lưu trữ dữ liệu. Thay vì sử dụng cơ sở dữ liệu quan hệ truyền thống như MySQL hay SQL Server, em quyết định sử dụng **Cơ sở dữ liệu Đồ thị Neo4j**.*
>
> *Em xin phép được phân tích lý do kỹ thuật đằng sau lựa chọn này:*
>
> *Trong CSDL quan hệ (SQL), dữ liệu được tổ chức thành các bảng độc lập. Khi chúng ta cần thực hiện các câu truy vấn tìm kiếm liên quan đến các mối quan hệ nhiều-nhiều hoặc truy vấn tìm kiếm sâu nhiều cấp — ví dụ như: 'Tìm những giảng viên có liên kết cộng tác với giảng viên A thông qua tối đa 3 bài báo trung gian' — hệ thống SQL bắt buộc phải thực hiện rất nhiều phép toán ghép bảng (JOIN) phức tạp. Các phép JOIN này đòi hỏi CPU quét các chỉ mục (Indexes) trên nhiều bảng khác nhau, gây ra hiện tượng suy giảm hiệu năng theo cấp số nhân khi dung lượng dữ liệu và độ sâu truy vấn tăng.*
>
> *Ngược lại, **Neo4j** hoạt động dựa trên nguyên lý **Index-free Adjacency (Kề cận không dùng chỉ mục)**. Trong Neo4j, mỗi nút (Node) lưu trữ trực tiếp địa chỉ bộ nhớ vật lý của các nút kề cận mà nó liên kết. Quá trình duyệt các mối quan hệ thực chất là quá trình lần theo các con trỏ bộ nhớ (Pointer hopping), không cần quét chỉ mục hay thực hiện phép JOIN nào. Độ phức tạp thời gian khi truy vấn quan hệ trong Neo4j chỉ phụ thuộc vào số lượng liên kết cần duyệt qua, chứ hoàn toàn độc lập với tổng quy mô của cơ sở dữ liệu.*
>
> *Bên cạnh đó, ngôn ngữ truy vấn **Cypher** của Neo4j được thiết kế dựa trên so khớp mẫu (Pattern Matching) trực quan bằng các ký hiệu trực quan giống như hình vẽ. Điều này giúp viết các truy vấn phức tạp cực kỳ ngắn gọn. Thay vì hàng chục dòng SQL lồng nhau, chúng ta chỉ cần một dòng Cypher duy nhất để tìm kiếm các mối quan hệ đệ quy phức tạp.*

---

## 📌 PHẦN 4: Thiết kế hệ thống & Lược đồ Đồ thị (≈ 3 phút)
*(Số từ: ~380 từ)*

**[Slide 5: Kiến trúc hệ thống 3 lớp]**

**Lời thoại:**
> *Tiếp theo, em xin phép trình bày về kiến trúc tổng thể của hệ thống. Ứng dụng được xây dựng theo mô hình kiến trúc 3 lớp tiêu chuẩn để đảm bảo tính module hóa và dễ bảo trì:*
>
> * **Lớp Trình diễn (Presentation Layer - Frontend):** Được xây dựng bằng HTML5, CSS3 nguyên bản (Vanilla CSS) và JavaScript ES6 để đảm bảo tốc độ tải trang nhanh và tối ưu SEO. Đối với việc trực quan hóa đồ thị tri thức, em sử dụng thư viện **Vis.js** để dựng môi trường tương tác vật lý thời gian thực.*
> * **Lớp Nghiệp vụ (Application Layer - Backend):** Sử dụng **Flask (Python)** làm nền tảng xử lý logic và cung cấp các dịch vụ RESTful API. Backend chịu trách nhiệm điều phối dữ liệu, xác thực người dùng bằng mã token JWT, kết nối tới cơ sở dữ liệu đồ thị qua Neo4j Python Driver, và tích hợp API của Google Gemini để xử lý ngôn ngữ tự nhiên.*
> * **Lớp Dữ liệu (Data Layer):** Sử dụng cơ sở dữ liệu đồ thị Neo4j để lưu trữ toàn bộ các thực thể học thuật dưới dạng nút và cạnh.*

**[Slide 6: Lược đồ dữ liệu đồ thị (Neo4j Schema)]**

**Lời thoại:**
> *Trên slide hiện tại là lược đồ đồ thị (Schema) được thiết kế cho hệ thống:*
>
> * Chúng ta có nút trung tâm là **GiangVien** (Giảng viên). Nút này kết nối tới nút **BoMon** (Bộ môn) qua quan hệ `THUOC_BO_MON`, và bộ môn kết nối tới **Khoa** qua quan hệ `THUOC_KHOA`.
> * Khi giảng viên viết bài báo khoa học, hệ thống tạo liên kết đến nút **CongTrinhNghienCuu** qua quan hệ `TAC_GIA_CHINH` hoặc `CONG_SU`.
> * Đối với đề tài nghiên cứu khoa học, giảng viên kết nối đến nút **DeTaiNghienCuu** bằng quan hệ `CHU_NHIEM` hoặc `THAM_GIA`.
> * Để ghi nhận các chuyên môn sâu, giảng viên kết nối đến nút **LinhVucNghienCuu** qua quan hệ `NGHIEN_CUU`.
> * Đặc biệt, để ghi nhận sự đóng góp của các nhà khoa học ngoài đơn vị, hệ thống hỗ trợ nút **TacGiaNgoai** kết nối đồng tác giả qua quan hệ `DONG_TAC_GIA` tới công trình hoặc đề tài.
>
> *Thiết kế này cho phép bản đồ tri thức có thể mở rộng bất kỳ nhãn nút hoặc quan hệ mới nào trong tương lai mà không cần cấu trúc lại toàn bộ cơ sở dữ liệu.*

---

## 📌 PHẦN 5: Các giải pháp kỹ thuật & Luồng hoạt động (≈ 4 phút)
*(Số từ: ~550 từ)*

**[Slide 7: Cơ chế hoạt động của Chatbot AI (Graph-RAG)]**

**Lời thoại:**
> *Sau đây, em xin đi sâu vào phần giải pháp kỹ thuật cốt lõi. Đầu tiên là **Quy trình hỏi đáp của Chatbot AI áp dụng mô hình Graph-RAG (Retrieval-Augmented Generation)**.*
>
> *Khi người dùng đặt một câu hỏi bằng tiếng Việt tự nhiên, ví dụ: 'Năm ngoái có bao nhiêu đề tài cấp Bộ?', luồng xử lý diễn ra qua 5 bước sau:*
>
> * **Bước 1:** Backend tiếp nhận câu hỏi và sử dụng hệ thống lọc Quy tắc (Rule-based) kết hợp thư viện xử lý ngôn ngữ tự nhiên Pyvi để phân tích ý định (Intent) và trích xuất thực thể (như tên giảng viên, năm, từ khóa lĩnh vực). Nếu không thuộc bộ quy tắc có sẵn, hệ thống gọi API của Gemini AI để phân tích ý định nâng cao.
> * **Bước 2:** Dựa vào ý định và thực thể đã xác định, backend sẽ gọi câu lệnh Cypher tương ứng để gửi yêu cầu truy vấn đến CSDL Neo4j.
> * **Bước 3:** CSDL Neo4j thực thi lệnh Cypher và trả về tập dữ liệu thô (gồm thông tin thuộc tính của nút và các liên kết).
> * **Bước 4:** Để tránh việc trả lại dữ liệu thô khó hiểu cho người dùng, backend gửi tập dữ liệu thô này kèm theo câu hỏi gốc quay lại cho Gemini AI cùng một chỉ thị: 'Hãy sử dụng dữ liệu chính xác này để biên soạn một câu trả lời tự nhiên, thân thiện và chính xác bằng tiếng Việt'.
> * **Bước 5:** Backend nhận câu trả lời dạng văn bản từ Gemini AI, đồng thời phân tích các thực thể được nhắc đến trong câu trả lời để truy vấn thêm một **đồ thị con 1-hop (Sub-graph)** xung quanh chúng. Cả câu trả lời dạng văn bản và dữ liệu đồ thị con này được đóng gói gửi về Frontend. Frontend dùng thư viện Vis.js để vẽ ngay một đồ thị mạng lưới nhỏ trực quan ngay dưới tin nhắn chat.
>
> *Như vậy, Graph-RAG giúp giải quyết triệt để hiện tượng 'ảo giác' (hallucination) của AI vì câu trả lời được neo chặt vào dữ liệu thực tế từ cơ sở dữ liệu đồ thị.*

**[Slide 8: Quy trình kiểm duyệt dữ liệu & ETL Import Excel]**

**Lời thoại:**
> *Giải pháp kỹ thuật tiếp theo giải quyết bài toán nghiệp vụ thực tế là **Quy trình Phê duyệt thông tin (Maker-Checker)** và **Tải dữ liệu hàng loạt**.*
>
> *Đối với việc Giảng viên cập nhật hồ sơ cá nhân:*
> *Để tránh việc dữ liệu học thuật bị chỉnh sửa tùy tiện hoặc sai lệch thông tin công bố, hệ thống không ghi đè trực tiếp các thay đổi của giảng viên vào thuộc tính gốc. Thay vào đó, backend tạo các trường đệm tạm thời (ví dụ: `pending_hoc_vi`) và đổi trạng thái cập nhật thành 'Chờ duyệt'. Admin sẽ duyệt bằng cách đối chiếu thông tin cũ và thông tin đề xuất. Khi duyệt, backend chạy Cypher chuyển dữ liệu từ trường tạm sang trường chính và xóa trường tạm.
>
> *Đối với việc Nhập dữ liệu hàng loạt từ Excel:*
> *Backend sử dụng thư viện **Pandas** để phân tích bảng dữ liệu. Quá trình import sử dụng cú pháp `MERGE` trong Cypher dựa trên mã giảng viên hoặc mã công trình duy nhất để tự động cập nhật nếu đã tồn tại, tránh trùng lặp dữ liệu.
>
> *Đặc biệt, hệ thống xử lý thông minh trường hợp Giảng viên chuyển công tác bằng cách tự động đổi nhãn nút từ `GiangVien` thành `TacGiaNgoai` và xóa quan hệ hành chính với bộ môn, nhưng giữ lại toàn bộ liên kết công trình cũ để bảo toàn dữ liệu lịch sử nghiên cứu của Khoa.*

---

## 📌 PHẦN 6: Tổng kết & Kết luận (≈ 1 phút)
*(Số từ: ~160 từ)*

**[Slide 9: Kết quả đạt được, Hạn chế & Hướng phát triển]**

**Lời thoại:**
> *Cuối cùng, em xin phép được tổng kết lại các kết quả đạt được của đồ án:*
>
> * **Kết quả:** Đồ án đã xây dựng thành công hệ thống bản đồ tri thức hoàn chỉnh, mượt mà trên Vis.js; triển khai giải pháp chatbot Graph-RAG thông minh hỗ trợ trả lời bằng tiếng Việt tự nhiên; và chuẩn hóa quy trình vận hành bảo mật bằng phân quyền 3 lớp vai trò.
> * **Hạn chế:** Hệ thống Chatbot vẫn phụ thuộc vào chất lượng đường truyền của API Gemini bên thứ ba và khả năng vẽ đồ thị Client-side bằng Vis.js có thể bị chậm nếu hiển thị đồng thời quá 1.000 đối tượng.
> * **Hướng phát triển:** Em mong muốn tích hợp thêm các thuật toán phân tích đồ thị nâng cao của Neo4j như PageRank hoặc phát hiện cụm (Louvain Community Detection) để gợi ý các nhóm nghiên cứu tiềm năng, cũng như kết nối tự động với CSDL lý lịch khoa học cấp trường.*
>
> *Kính thưa Hội đồng, trên đây là toàn bộ nội dung thuyết trình lý thuyết và giải pháp công nghệ của đồ án "Xây dựng Knowledge Map phục vụ nghiên cứu khoa học tại Khoa CNTT". Em xin chân thành cảm ơn quý thầy cô đã lắng nghe và em xin phép được tiếp nhận các câu hỏi phản biện từ Hội đồng.*

---

## 💡 Gợi ý chuẩn bị thêm của bạn khi thuyết trình:
1. **Luyện tập ngắt nghỉ:** Hãy thử đọc to kịch bản này 1-2 lần để đo chính xác thời gian nói của bạn. Nếu bạn nói nhanh, hãy cố gắng hạ tốc độ xuống và nhấn mạnh vào các thuật ngữ chuyên môn như: **CSDL Đồ thị (Neo4j)**, **Index-free Adjacency (Kề cận không dùng chỉ mục)**, **Graph-RAG**, **Maker-Checker**.
2. **Chuẩn bị Slide khớp kịch bản:** Đảm bảo các Slide từ 1 đến 9 chứa đúng các từ khóa và hình vẽ cấu trúc hệ thống, sơ đồ Schema Neo4j và sơ đồ quy trình Graph-RAG như được mô tả trong kịch bản nói để tăng tính thuyết phục.

---

## 💡 CÁC CÂU HỎI PHẢN BIỆN HỘI ĐỒNG VÀ GỢI Ý TRẢ LỜI CHI TIẾT

Dưới đây là các câu hỏi chuyên môn rất dễ bị hỏi bởi Hội đồng chấm đồ án, kèm theo gợi ý trả lời ngắn gọn, thông minh và mang tính kỹ thuật cao:

### ❓ Câu 1: Tại sao em lại chọn Cơ sở dữ liệu Đồ thị Neo4j mà không chọn các CSDL quan hệ quen thuộc (SQL) hay Document-based (MongoDB)?
* **Gợi ý trả lời:**
  > *"Dạ thưa thầy/cô, lý do chính là do **cấu trúc liên kết sâu và đa chiều** của dữ liệu nghiên cứu khoa học. Trong hệ thống học thuật, các mối liên kết (như cộng sự của cộng sự, bài viết của giảng viên thuộc bộ môn X nghiên cứu lĩnh vực Y) có tính kết nối rất cao.*
  >
  > *Nếu sử dụng CSDL quan hệ (SQL), để truy vấn các mối quan hệ nhiều-nhiều sâu từ 2-3 cấp trở lên, hệ thống sẽ phải thực hiện các phép toán `JOIN` liên tiếp trên nhiều bảng trung gian. Điều này gây tốn CPU và RAM để quét chỉ mục của toàn bộ các bảng liên quan, làm giảm hiệu năng truy vấn nghiêm trọng.*
  >
  > *Nếu sử dụng MongoDB (CSDL tài liệu), dữ liệu bị phân mảnh và chúng ta phải tự thực hiện phép tham chiếu thủ công ở tầng code ứng dụng, rất phức tạp và dễ lỗi.*
  >
  > *Neo4j giải quyết triệt để vấn đề này nhờ nguyên lý **Index-free Adjacency (Kề cận không dùng chỉ mục)**. Các nút được liên kết trực tiếp với nhau bằng địa chỉ bộ nhớ vật lý của con trỏ. Khi truy vấn mối quan hệ, Neo4j chỉ duyệt theo các con trỏ kề cận mà không cần quét chỉ mục hay ghép bảng, giúp tốc độ truy vấn ổn định ở mức mili-giây ngay cả khi lượng dữ liệu phình to."*

### ❓ Câu 2: Trong phần Chatbot AI, cơ chế Graph-RAG của em hoạt động thế nào? Tại sao lại gọi là Graph-RAG mà không phải RAG thông thường?
* **Gợi ý trả lời:**
  > *"Dạ thưa thầy/cô, trong RAG (Retrieval-Augmented Generation) thông thường, tài liệu được chia nhỏ thành các đoạn văn bản (chunks), chuyển đổi thành các vector rồi lưu vào Vector Database. Khi người dùng hỏi, hệ thống tìm các chunk có độ tương đồng vector gần nhất rồi gửi cho LLM để sinh câu trả lời.*
  >
  > *Tuy nhiên, phương pháp RAG thông thường gặp hạn chế lớn khi câu hỏi yêu cầu **tổng hợp thông tin kết nối** (ví dụ: 'Thống kê số bài báo của bộ môn Kỹ thuật phần mềm'). Các đoạn văn bản rời rạc không thể cung cấp bức tranh toàn cảnh về quan hệ kết cấu.*
  >
  > *Với **Graph-RAG** trong đồ án của em, hệ thống sử dụng kết hợp:*
  > * *Bước 1: LLM phân tích ý định và dịch câu hỏi thành câu lệnh truy vấn cấu trúc đồ thị (Cypher).*
  > * *Bước 2: Truy vấn trực tiếp Neo4j để lấy chính xác các Nút và Quan hệ liên quan (dữ liệu có cấu trúc 100% chính xác).*
  > * *Bước 3: Gửi dữ liệu chính xác này cho LLM diễn giải thành câu trả lời tự nhiên.*
  >
  > *Do đó, câu trả lời của Chatbot đồ án em luôn đảm bảo **độ chính xác tuyệt đối**, loại bỏ hoàn toàn hiện tượng ảo giác (hallucination) thường gặp của AI thông thường."*

### ❓ Câu 3: Nếu API Gemini AI bị lỗi mạng, bị quá giới hạn lượt gọi (rate limit) hoặc mất kết nối thì Chatbot của em có hoạt động được không?
* **Gợi ý trả lời:**
  > *"Dạ thưa thầy/cô, hệ thống của em đã được thiết kế sẵn một **cơ chế dự phòng tự động (Fallback)** tại backend:*
  >
  > *Nếu API Gemini gặp sự cố, backend Flask sẽ bắt lỗi ngoại lệ (Exception) và tự động kích hoạt bộ **phân tích quy tắc nội bộ (Rule-based)** sử dụng kỹ thuật tìm kiếm mờ (Fuzzy matching) và so khớp từ khóa qua Regex.*
  >
  > *Hệ thống sẽ tự động chuyển câu hỏi thành các truy vấn Cypher định sẵn dựa trên các từ khóa (ví dụ: nhận dạng tên giảng viên, tên bộ môn hoặc năm) và hiển thị kết quả trực tiếp dưới dạng danh sách hoặc bảng biểu tĩnh cho người dùng. Điều này đảm bảo Chatbot hoạt động ổn định 24/7 và không bao giờ bị sập hoàn toàn."*

### ❓ Câu 4: Làm thế nào em đảm bảo dữ liệu không bị trùng lặp hoặc phân mảnh khi Admin thực hiện nhập dữ liệu hàng loạt (Import) từ file Excel?
* **Gợi ý trả lời:**
  > *"Dạ thưa thầy/cô, khi lập trình module import Excel, em đã áp dụng hai kỹ thuật chính:*
  >
  > * **Thứ nhất, sử dụng lệnh `MERGE` thay vì `CREATE` trong Cypher:** Lệnh `MERGE` sẽ kiểm tra thuộc tính định danh duy nhất (ví dụ: mã giảng viên `ma_gv`, mã bài báo `id`) trên cơ sở dữ liệu đồ thị. Nếu thực thể đã tồn tại, hệ thống chỉ cập nhật các thông tin thay đổi (`ON MATCH SET`). Nếu chưa tồn tại, hệ thống mới tạo nút mới (`ON CREATE SET`).*
  > * **Thứ hai, thiết lập cơ chế xử lý lỗi theo từng dòng (Row-by-Row Validation):** Backend sử dụng Pandas để duyệt qua từng dòng trong file Excel. Nếu một dòng dữ liệu bị lỗi định dạng hoặc thiếu thông tin bắt buộc, backend sẽ bỏ qua dòng đó, ghi nhận lỗi vào file nhật ký lỗi (error log) để phản hồi lại cho Admin, và tiếp tục import các dòng hợp lệ tiếp theo. Điều này giúp tránh việc toàn bộ file import bị hủy bỏ chỉ vì một vài dòng dữ liệu bị lỗi."*

### ❓ Câu 5: Tại sao em không cho phép Giảng viên tự cập nhật trực tiếp hồ sơ của họ lên Bản đồ Tri thức mà phải qua quy trình phê duyệt (Maker-Checker) phức tạp?
* **Gợi ý trả lời:**
  > *"Dạ thưa thầy/cô, đây là hệ thống quản lý học thuật chính thống của Khoa/Trường, dữ liệu công khai trên Bản đồ Tri thức có ảnh hưởng trực tiếp đến uy tín nghiên cứu khoa học của đơn vị.*
  >
  > *Nếu cho phép giảng viên tự do sửa đổi và cập nhật trực tiếp, hệ thống sẽ gặp các rủi ro lớn:*
  > * *Khai báo sai lệch thông tin (ví dụ: tự nâng học vị hoặc ghi nhận nhầm bài báo).*
  > * *Rủi ro bảo mật: Nếu tài khoản giảng viên bị kẻ xấu xâm nhập, chúng có thể phá hoại hoặc xóa bỏ dữ liệu nghiên cứu khoa học.*
  >
  > *Vì vậy, em áp dụng mô hình phê duyệt hai bước **Maker-Checker**: Giảng viên chỉ đóng vai trò người đề xuất (Maker), dữ liệu thay đổi được lưu tạm ở các thuộc tính đệm. Chỉ khi Quản trị viên (Checker) kiểm tra tính hợp lệ và phê duyệt, dữ liệu mới chính thức được cập nhật lên đồ thị."*

### ❓ Câu 6: Thư viện Vis.js dựng đồ thị ở phía Client (Trình duyệt). Nếu số lượng giảng viên, công trình tăng lên đến hàng ngàn nút thì giao diện có bị đơ/lag không? Em tối ưu hiệu năng thế nào?
* **Gợi ý trả lời:**
  > *"Dạ thưa thầy/cô, đây là một thách thức thực tế về hiệu năng Frontend. Vis.js mặc định sử dụng thuật toán vật lý (physics simulation) liên tục để tính toán lực đẩy Coulomb giữa các nút nhằm dàn đều đồ thị.*
  >
  > *Để tối ưu hiệu năng, em đã áp dụng ba giải pháp:*
  > * * **Giới hạn số lượng Node vẽ ra:** Hệ thống không load toàn bộ hàng ngàn node lên cùng lúc. Trang khám phá mặc định chỉ hiển thị mạng lưới rút gọn. Người dùng dùng bộ lọc bộ môn hoặc tìm kiếm để khoanh vùng dữ liệu cần xem.*
  > * * **Tắt hiệu ứng vật lý sau khi ổn định (Stabilization):** Em thiết lập cấu hình Vis.js chỉ chạy tính toán lực vật lý tối đa 200 vòng lặp (iterations) khi khởi tạo để đồ thị dàn đều, sau đó tự động tắt hiệu ứng vật lý (`physics: false`). Khi người dùng kéo thả các nút, trình duyệt không phải tính toán lại lực kéo cho các nút khác, giúp giảm tải CPU/GPU.*
  > * * **Phân trang dữ liệu mạng lưới:** Đối với các nút quá dày đặc, hệ thống hỗ trợ gom nhóm (Clustering) hoặc chỉ hiển thị các mối quan hệ trực tiếp 1-hop khi người dùng click vào nút cụ thể."*

