/* ============================================================
   CONFIG - Global constants & shared state
   ============================================================ */

const API_BASE = '/api';

// Chart instances (managed by statistics.js)
let chartTrend = null;
let chartLevel = null;
let chartDept  = null;

// Graph instances (managed by graph.js)
let dashboardGraph = null;
let exploreGraph   = null;
